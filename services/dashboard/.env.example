# --- Database (required) ---
# Same straw-backed Postgres the vision service writes detections to.
DB_URL=postgresql://user:pass@host:5432/caffeinated

# --- Dashboard server ---
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8050
DASHBOARD_POLL_MS=2000   # how often the browsers re-poll for new defects
