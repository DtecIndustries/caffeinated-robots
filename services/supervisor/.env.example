# --- Soda Straw MCP gateway + scoped agent key ---
# STRAW_API_KEY is the API key minted for the `production-line-supervisor` agent
# (scoped to the postgres + discord-bot straws). Do NOT commit the real value.
STRAW_MCP_URL=https://kustex.straw.demo.soda.io/mcp
STRAW_API_KEY=
POSTGRES_STRAW=postgres
DISCORD_STRAW=discord-bot
DB_NAME=caffeinated

# --- Discord ---
DISCORD_CHANNEL_ID=1510229539551314054
DISCORD_BOT_USER_ID=1510251416457838784

# --- Anthropic (supervisor assistant) ---
ANTHROPIC_API_KEY=
SUPERVISOR_MODEL=claude-opus-4-8
SUPERVISOR_EFFORT=medium

# --- Loop cadence (seconds) ---
MONITOR_INTERVAL=5
DISCORD_POLL_INTERVAL=4
