/** Single human-review holding bay — maps to production_line.pos5 */
export const HOLDING_DEF = {
  id: 'holding',
  label: 'Holding',
};

/** Reject / no-go bay — opposite side of holding; pos6 when present else no-go tags */
export const REJECT_DEF = {
  id: 'reject',
  label: 'Reject',
};
